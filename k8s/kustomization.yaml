apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: finnhub

resources:
  - 00-namespace.yaml
  - 02-configmap-postgres-init.yaml
  - 03-postgres.yaml
  - 04-kafka.yaml
  - 05-kafka-topics.yaml

secretGenerator:
  - name: finnhub-secrets
    envs:
      - ../.env
